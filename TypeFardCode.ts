console.log("fard")
var fard = true {
    let fard = "brrr";
}
let fard: string
var string = true

interface Point {
    x: number;
    y: number;
}

function logPoint(p: Point) {
    console.log(`${p.x}, ${p.y}`);
}

// logs "12, 26"
const point = { x: 12, y: 26 };
logPoint(point);

var logpoint = true

let logpoint: const user: User = new UserAccount("Helloworld", 1)